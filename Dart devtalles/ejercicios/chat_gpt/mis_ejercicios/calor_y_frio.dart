// Uso de variables en estructuras de control:

// Declara una variable entera llamada "temperatura" y asígnale un valor.

// Utiliza una estructura de control "if-else" para

//determinar si hace frío o calor según el valor de
//la variable "temperatura". Considera que hace frío
//si la temperatura es menor a 20 grados y hace calor
//en caso contrario.

// Imprime el resultado en la consola.

int temperatura = 21;
main() {
  if (temperatura > 20) {
    print('hace Calor');
  } else if (temperatura < 20) {
    print('hace frio');
  } else {
    print('del carajo');
  }
}
