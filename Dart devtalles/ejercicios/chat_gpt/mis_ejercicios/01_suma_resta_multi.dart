// Declaración y asignación de variables:

// Declara una variable de tipo entero llamada "edad" y asígnale tu edad.

int edad = 45;
// Declara una variable de tipo double llamada "altura" y asígnale tu altura en metros.
double altura = 1.72;
// Declara una variable de tipo String llamada "nombre" y asígnale tu nombre.

String nombre = 'Javier';

// Declara una variable de tipo bool llamada "esEstudiante" y asígnale un valor verdadero o falso según si eres estudiante o no.
bool esEstudiante = true;

// Operaciones con variables:

// Declara dos variables enteras "num1" y "num2" y asígnales valores.

int num1 = 2;
int num2 = 5;

// Realiza la suma, resta, multiplicación y división de "num1" y "num2" y almacena los resultados en variables separadas.

int restaEjer(a, b) {
  return a - b;
}

int sumaEjer(a, b) {
  return a + b;
}

int multiEjer(a, b) {
  return a * b;
}

double diviEjer(a, b) {
  return a / b;
}

int suma = sumaEjer(num1, num1);
int resta = restaEjer(num1, num2);
int multi = multiEjer(num1, num2);
double divi = diviEjer(num1, num2);

// Imprime los resultados de las operaciones en la consola.
main() {
  print(suma);
  print(resta);
  print(multi);
  print(divi);
}
// Interpolación de variables en cadenas de texto:

// Utiliza las variables "nombre" y "edad" del ejercicio 1.

// Crea una cadena de texto que incluya ambas variables utilizando la interpolación de variables.

// Imprime la cadena de texto en la consola.

// Uso de variables en estructuras de control:

// Declara una variable entera llamada "temperatura" y asígnale un valor.

// Utiliza una estructura de control "if-else" para determinar si hace frío o calor según el valor de la variable "temperatura". Considera que hace frío si la temperatura es menor a 20 grados y hace calor en caso contrario.

// Imprime el resultado en la consola.


